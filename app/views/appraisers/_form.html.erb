<%= form_with(model: appraiser, local: true) do |form| %>
  <% if appraiser.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(appraiser.errors.count, "error") %> prohibited this appraiser from being saved:</h2>

      <ul>
      <% appraiser.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
